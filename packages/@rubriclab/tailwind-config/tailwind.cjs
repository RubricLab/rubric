const colors = require("tailwindcss/colors");

module.exports = {
  theme: {
    extend: {
      colors: {
        ...colors,
        "off-white": "#E2DCDB",
        transparent: "transparent",
      },
    },
  },
  plugins: [],
};
