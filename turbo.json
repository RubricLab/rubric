{
	"$schema": "https://turbo.build/schema.json",
	"tasks": {
		"build": {
			"dependsOn": ["^build"],
			"outputs": ["!.next/cache/**", ".next/**", "dist/**"]
		},
		"dev": {
			"cache": false,
			"persistent": true
		},
		"clean": {},
		"bleed": {},
		"format": {}
	}
}
